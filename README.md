# CSSECDV
case study group 7

prerequisites
1. clone the repo
2. `cd tiggets`
3. `npm.cmd install` or `npm install` to get node_modules

to run the app 
1. `cd tiggets` (make sure to always run npm commands inside this folder)
2. `npm.cmd run dev` or `npm run dev`




jack's notes remove this later
```tsx
// in page.tsx
      <main className="flex flex-1 w-full max-w-3xl flex-col items-center justify-between py-32 px-16 sm:items-start bg-background">
        <h1>test</h1>
      </main>
```

```tsx
export default function Home() {
  return (
    <div className="flex flex-col flex-1 items-center justify-center bg-[url('/Login-bg.png')] bg-cover bg-center bg-fixed font-text">
      <LoginForm />
    </div>
  );
}
```