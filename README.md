# CSSECDV
case study group 7

prerequisites
1. clone the repo
2. `cd tiggets`
3. `npm.cmd install` or `npm install` to get node_modules

to run the app 
1. `cd tiggets` (make sure to always run npm commands inside this folder)
2. `npm.cmd run dev` or `npm run dev`
